{
  "name": "voyage-api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "node scripts/build.mjs",
    "start": "node src/server.mjs",
    "test": "node --test",
    "lint": "node --check src/server.mjs && node --check src/config.mjs && node --check src/availability.mjs"
  }
}
